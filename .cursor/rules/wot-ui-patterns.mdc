---
description: 页面开发必须优先使用 wot-ui（wd-*）组件，仅特殊布局可例外
globs: src/**/*.vue
alwaysApply: false
---

# wot-ui 组件使用规范

本项目 UI 库为 **wot-ui**（`@wot-ui/ui`，前缀 `wd-`）。easycom 已配置，**无需手动 import**，直接在模板中使用。

## 核心原则

**能用 wot-ui 的必须用，禁止用原生标签或手写样式重复造轮子。**

仅以下情况允许不用 wot-ui：

- **布局容器**：`view`、`scroll-view`、`canvas`、`image` 等结构性/平台能力元素
- **品牌/营销页**：首页大按钮、渐变背景、自定义 Hero 区等强视觉设计
- **wot-ui 无对应能力**：如 `uni.scanCode`、地图、视频等原生 API 场景

拿不准时，先查 [wot-ui 文档](https://wot-design-uni.cn/) 再写代码。

## 组件对照（必须遵守）

| 场景 | ❌ 禁止 | ✅ 使用 |
|------|--------|--------|
| 按钮 | `<button>` | `wd-button` |
| 表单 | `wd-cell-group` + `wd-cell` 手写表单 | `wd-form` + `wd-form-item`（数据录入、校验） |
| 输入框 | `<input>` | `wd-input` |
| 列表/只读展示 | 手写 flex 行 | `wd-cell` + `wd-cell-group`（历史列表、工具入口等非表单场景） |
| 弹层/抽屉 | 手写遮罩 div | `wd-popup` |
| 确认/提示框 | 仅 `uni.showModal` | `useMessage` / `MessageBox`（简单场景可用 `uni.showModal`） |
| Toast | 仅 `uni.showToast` | `useToast`（简单一行提示可用 `uni.showToast`） |
| 空状态 | 手写「暂无数据」 | `wd-empty` |
| 标签/角标 | 手写 span 样式 | `wd-tag` / `wd-badge` |
| 选择器 | 原生 `<picker>` | `wd-picker` / `wd-select-picker` / `wd-picker-view` |
| 开关/复选/单选 | 原生 switch/checkbox | `wd-switch` / `wd-checkbox` / `wd-radio` |
| 通知条 | 手写提示条 | `wd-notice-bar` |
| 滑动操作 | 手写侧滑 | `wd-swipe-action` |
| 加载中 | 手写 loading | `wd-loading` |
| 图标 | emoji/文字代替 | `wd-icon` |
| 分割线 | 手写 border | `wd-divider` |

## 表单写法（必须遵守）

数据录入页面使用 **`wd-form` + `wd-form-item`**，不再用 `wd-cell-group` + `wd-cell` 拼表单。

- `wd-form` 绑定 `:model`，需要校验时传 `:schema`（自定义 `FormSchema` 或 `zodAdapter`）
- 公共样式在 `wd-form` 上配置：`center`、`value-align="right"`、`:title-width="100"`、`border`；多区块时可内嵌 `wd-cell-group`
- `wd-form-item` 用 `prop` 对应 model 字段；**单列选择**用 `is-link` + `:value` 触发，配合外部 `wd-picker`（`v-model` 为数组、`v-model:visible` 控制弹层）
- 提交前调用 `formRef.validate()`，简单提示用 `error-type="toast"`

```vue
<script lang="ts" setup>
import type { FormSchema } from '@wot-ui/ui'
import type { FormExpose } from '@wot-ui/ui/components/wd-form/types'

const formRef = ref<FormExpose>()
const formModel = reactive({ ssid: '', password: '', hidden: false })

const formSchema: FormSchema = {
  validate(model) {
    if (!String(model.ssid ?? '').trim())
      return [{ path: ['ssid'], message: '请输入 WiFi 名称' }]
    return []
  },
  isRequired(path) {
    return path === 'ssid'
  },
}

async function submit() {
  const { valid } = await formRef.value!.validate()
  if (!valid) return
  // ...
}
</script>

<template>
  <wd-form
    ref="formRef"
    :model="formModel"
    :schema="formSchema"
    center
    border
    value-align="right"
    error-type="toast"
    :title-width="100"
    custom-class="card-rounded"
  >
    <wd-form-item title="WiFi 名称" prop="ssid">
      <wd-input v-model="formModel.ssid" align-right placeholder="请输入 WiFi 名称" />
    </wd-form-item>
    <wd-form-item title="密码" prop="password">
      <wd-input v-model="formModel.password" align-right show-password placeholder="无密码可留空" />
    </wd-form-item>
    <wd-form-item title="是否为隐藏WiFi" prop="hidden">
      <wd-switch v-model="formModel.hidden" size="20" />
    </wd-form-item>
  </wd-form>
</template>
```

## 样式约定

- 组件外观优先用 wot-ui 的 `type`、`size`、`variant`（如 `variant="plain"`）、`round` 等 props 控制
- 布局与间距用 **UnoCSS** 原子类（见 `styling-css-patterns.mdc`）
- 需要微调时用 `custom-class` / `custom-style`，不要覆盖组件内部结构
- 表单内 `is-link` 选择行与输入框混排时，给 `.wd-cell.is-link .wd-cell__body` 设 `min-height: var(--wot-input-inner-height)`

## 参考实现

- WiFi 生成表单：`src/pages/generate/generate.vue`
- 复杂表单 + 弹层：`src/pages-sub/salary/calc.vue`
- 列表 + 空态 + 滑动删除：`src/pages-sub/salary/history.vue`
