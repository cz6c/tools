WiFi二维码助手 - 产品需求文档（PRD）
版本：v1.0
目标平台：微信小程序（使用 Uniapp 开发）
目标用户：C端个人用户
文档用途：为开发（含AI辅助编码）提供清晰、无歧义的需求定义，不包含具体代码实现。

一、产品概述
产品名称：WiFi小助手（暂定）
一句话描述：一款帮助用户快速扫码连接WiFi，并生成个人WiFi二维码的工具。
核心价值：免除手动输入密码的繁琐，方便分享网络。

适用范围：家庭、朋友聚会、小型办公等场景。

二、目标用户
普通家庭用户：想给来访客人快速连网，无需口头告知密码。

小型活动组织者：临时共享WiFi给参与者。

对隐私敏感的用户：不愿频繁透露密码，通过扫码或二维码分享。

三、功能模块总览
模块	核心功能	优先级
扫码连接	扫描WiFi二维码，解析并展示信息，引导连接	P0（必须）
生成二维码	输入SSID/密码/加密类型，生成可保存的二维码图片	P0
历史记录	保存最近扫描和生成的WiFi配置，方便复用	P1（重要）
P0：首版必须完成；P1：首版尽量包含。

四、详细功能需求
4.1 首页
入口：小程序启动后的第一个页面。
页面元素：

应用标题（如“WiFi小助手”）

两个核心操作按钮：

“扫一扫”（主按钮，突出显示）

“生成我的WiFi码”（次按钮）

若有历史记录，底部显示“历史记录”入口（带数量提示可选）

交互：

点击“扫一扫”：调用系统相机扫码（仅二维码），获取扫描结果。

点击“生成我的WiFi码”：跳转至生成页面。

点击“历史记录”：跳转至历史列表页面。

异常处理：

若用户拒绝相机权限，提示“需要相机权限才能扫码”，引导至设置。

4.2 扫码结果页
触发：首页扫码成功后，携带扫描到的原始文本跳转至此页。
页面内容：

解析后的WiFi信息展示：

网络名称（SSID）：明文显示

密码：默认隐藏（显示为圆点或星号），提供一个“显示/隐藏”切换按钮

加密类型（如WPA2）

两个操作按钮：

“复制密码”：将密码复制到剪贴板，成功后Toast提示

“连接WiFi”：尝试自动连接该网络（仅微信小程序环境）

若解析失败（非标准WiFi格式），显示“无效的WiFi二维码”提示，并提供返回首页按钮。

连接WiFi的交互流程：

调用微信的WiFi连接能力。

若连接成功：提示“连接成功”。

若连接失败（如iOS/Android限制、密码错误、WiFi未开启等）：

弹出模态框，显示“连接失败，请前往系统设置手动连接”，并告知SSID和密码（密码可查看）。

提供“复制密码”快捷操作。

数据存储：扫描成功后，自动将此次WiFi信息存入历史记录（标记为“扫码”类型），避免重复存储（若已有相同SSID+密码则更新时间）。

4.3 生成二维码页
入口：首页点击“生成我的WiFi码”。
页面内容：

表单区域：

SSID输入框（文本，必填）

密码输入框（文本，可切换明文/密文，非必填，若为空表示无密码）

加密类型选择器（下拉或选择器，选项：WPA/WPA2/WPA3/WEP/无密码，默认WPA2）

“生成二维码”按钮（点击后触发）

二维码展示区域（生成后显示）：

一个Canvas绘制的二维码图片

二维码下方显示“保存到相册”和“分享”按钮（分享暂可简化为引导用户保存后自行发送）

生成逻辑：

根据用户输入，按照标准WiFi二维码格式（WIFI:T:加密类型;S:SSID;P:密码;;）生成文本。

使用前端库将该文本绘制为二维码图片。

生成后自动将该配置存入历史记录（标记为“生成”类型）。

校验：

SSID不能为空，否则提示“请输入WiFi名称”。

若选择加密类型为“无密码”，则密码可为空。

保存图片：调用系统相册保存权限，将二维码Canvas转为图片保存。若权限未授权，引导授权。

4.4 历史记录页
入口：首页底部“历史记录”。
页面内容：

列表展示所有历史条目（按时间倒序）。

每条条目显示：

SSID（主标题）

加密类型（小标签）

记录类型（“扫码”或“生成”）

时间（如“昨天 15:30”）

右上角“清空”按钮（清空全部历史，需二次确认）。

交互：

点击任意条目：跳转至该WiFi的详情页（或直接进入生成页填充信息，亦可复用扫码结果页风格）。建议跳转至一个“详情页”，可查看完整信息（密码可显隐），并提供“复制密码”和“再次生成二维码”（如果是生成类型）或“再次扫码连接”（如果是扫描类型）操作。

为简化，可统一跳转至生成页并回填SSID和密码，让用户重新生成或修改。

数据存储：使用本地缓存（Storage），最多保存50条，超出时删除最旧条目。

五、页面与交互流程
5.1 页面列表
页面名称	路径	说明
首页	/pages/index	主入口
扫码结果	/pages/scanResult	展示解析结果和连接操作
生成二维码	/pages/generate	表单+生成展示
历史记录	/pages/history	列表展示
（可选）详情页	/pages/detail	查看历史条目的完整信息
建议最少4个页面，若详情复用生成页则只需4个。

5.2 用户核心流程
流程1：扫码连网

用户打开小程序 → 首页点击“扫一扫”

扫描二维码 → 自动解析

进入结果页，看到SSID和密码（密码隐藏）

点击“连接WiFi” → 系统尝试连接

成功：提示成功；失败：引导手动设置并提供复制密码

流程2：生成自己的WiFi二维码

用户打开小程序 → 首页点击“生成我的WiFi码”

填写SSID、密码、加密方式

点击“生成二维码” → 显示二维码图片

点击“保存到相册” → 保存图片

流程3：查看历史记录

首页点击“历史记录”

浏览列表，点击某条查看详情或重新生成

可清空全部记录

六、数据模型（本地存储）
存储键名：wifi_history
存储格式：JSON数组，每个对象包含：

字段	类型	说明
id	string	唯一标识（建议使用时间戳+随机数）
ssid	string	网络名称
password	string	密码（明文存储，因仅本地使用）
encryption	string	加密类型（如WPA2）
type	string	'scanned' 或 'generated'
createdAt	number	时间戳（毫秒）
操作：

添加：新增记录插入数组头部，去重（若相同ssid+password则更新时间）。

查询：按时间倒序返回。

删除：支持清空全部（暂不支持单条删除，可后续迭代）。

最大条数：50。

七、非功能需求
7.1 兼容性
支持微信小程序基础库 2.10.0 及以上。

适配主流iOS（12+）和Android（8.0+）机型。

7.2 性能
二维码生成时间不超过1秒。

页面切换流畅，无卡顿。

7.3 隐私合规
不需要用户手机号、位置等敏感信息。

仅申请必要的权限：相机（扫码）、相册（保存图片）、WiFi（连接）。

首次启动时需弹窗展示《用户协议》和《隐私政策》，明确说明密码仅本地存储，不上传服务器。

代码中不得收集任何用户数据。

7.4 易用性
按钮文字清晰，操作反馈明确（Toast、模态框）。

密码隐藏/显示切换直观。

错误提示友好，给出具体操作建议。

八、UI设计指南（简要）
风格：简洁、清爽，主色建议蓝色系（#007AFF）。

首页：两个大圆角按钮，图标+文字。

结果页：卡片式信息展示，操作按钮并排。

生成页：表单整洁，底部生成按钮。

历史记录：列表左对齐，右置时间标签。

设计师可参考微信原生组件风格，确保小程序体验一致。

九、版本规划
V1.0（MVP）：

扫码解析并展示，引导连接（含手动提示）

生成二维码并保存相册

历史记录（增删查）

V1.1（后续迭代）：

从相册选择二维码图片识别（需引入第三方库）

二维码样式自定义（颜色、Logo）

分享二维码图片至微信好友/朋友圈

十、验收标准
扫描标准WiFi二维码，能正确解析SSID和密码。

点击“连接WiFi”，在iOS和Android上均有明确反馈（成功或引导）。

生成二维码：输入信息后能得到可识别且内容正确的二维码。

保存二维码到相册成功。

历史记录正常存储、显示、清空。

无任何用户数据上报。

所有提示文案清晰、友好。

文档结束。本PRD已定义所有功能细节，未涉及任何代码实现，开发人员（或AI编程助手）可依据此文档编写代码。如有需求变更，请更新文档版本。
